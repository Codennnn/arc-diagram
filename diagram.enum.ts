export const enum NodeType {
  Database = 'database',
  Service = 'service',
  Users = 'users',
  Queue = 'queue',
}

export const enum EdgeType {
  Text = 'edge-text',
}

export const enum ContainerType {
  Multiple = 'multiple',
  Single = 'single',
  External = 'external',
  None = 'none',
}
